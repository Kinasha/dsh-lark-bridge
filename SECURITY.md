# 安全策略

## 支持的版本

本项目处于实验阶段，只对**最新发布版本**提供安全修复。当前版本与
`@deepseek-ai/dsh@0.1.0-rc.7` 对齐。

| 版本 | 是否支持 |
| --- | --- |
| 最新的 `0.0.x` 发布版 | ✅ |
| 更早的版本 | ❌ 请先升级 |

## 报告漏洞

**请不要在公开 issue 中报告安全漏洞。**

请使用 GitHub 的私密漏洞报告通道：进入仓库的
[Security 页面](https://github.com/Kinasha/dsh-lark-bridge/security/advisories/new)，
选择 “Report a vulnerability”。该通道只有仓库维护者可见。

报告时请尽量包含：

- 受影响的版本与运行方式（DSH 插件模式或 `standalone` 本地运行时）
- 复现步骤或最小复现用例
- 影响范围：能读到什么、能改到什么、需要哪些前置条件
- 你认为合适的修复方向（可选）

**请在报告中脱敏**：不要附上真实的 App Secret、用户 OAuth Token、open ID 或聊天
内容。

维护者会在 7 天内确认收到报告，并在修复发布前与你同步进展。请在修复发布前不要公开
披露细节。

## 威胁模型

本插件把一个**不受信任的聊天入口**（飞书消息）接到一个**本机运行、可以调用工具的
DSH Host** 上。使用时请注意以下边界：

- **飞书输入是数据，不是指令。** 消息内容不会被当作授权凭据；斜杠命令默认关闭
  （`DSH_LARK_ALLOW_SLASH_COMMANDS=0`）。
- **谁能触发执行由 sender 策略决定。** 生产环境请设置
  `DSH_LARK_ALLOWED_SENDERS`（必要时叠加 `DSH_LARK_BLOCKED_SENDERS`）；不设置时，
  任何能访问该飞书应用的用户都能触发本机执行。
- **从飞书批准工具调用默认关闭。** `DSH_LARK_APPROVALS_ENABLED=0`，因为批准一次工具
  调用等于授权修改运行 DSH 的这台机器。开启前请先确认 DSH 默认 Agent 的授权策略。
- **卡片回调按数据校验。** 会话必须由本进程创建、消息 ID 必须与渲染该按钮的卡片一
  致、操作者必须是话题发起人；非幂等操作还会消耗一次性 nonce。
- **凭证不落在仓库里。** App Secret 存放在 harness 凭证层（`ctx.credentials`），设置
  界面只显示是否已配置，永远不回显值；状态文件以 `0600` 原子写入。
- **设置 API 只在回环地址注册**，与 DSH 自身的配置面约束一致。
- **HTML 报告不做净化**，而是以 Content-Security-Policy 作为隔离边界，通过
  回环路由托管。

## 部署建议

- 不要把 DSH Web UI 或本插件的回环端口暴露到公网。
- 从具体项目目录启动 DSH，不要用家目录等大范围目录作为 Workspace。
- 不要在日志、issue 或截图中输出 App Secret 与用户 Token。
- 密钥泄露后先在飞书开放平台轮换，再处理仓库与日志。

## 不在范围内

- DSH 本体、飞书开放平台以及第三方依赖自身的漏洞（请向对应项目报告）
- 由部署方主动放宽配置导致的问题，例如把 Web UI 暴露到公网、关闭 sender 策略、或
  开启 `DSH_LARK_APPROVALS_ENABLED` 后授予 Agent 过宽的工具权限
